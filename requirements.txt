# ─────────────────────────────────────────────────────────────
# requirements.txt — 5G / 6G Network Intrusion Detection Project
# Covers: preprocessing.py, feature_engineering.py,
#         train.py, evaluate.py
# ─────────────────────────────────────────────────────────────

# Core data manipulation
numpy>=1.24.0
pandas>=2.0.0

# Visualisation
matplotlib>=3.7.0
seaborn>=0.12.0

# Machine learning (scikit-learn ecosystem)
scikit-learn>=1.3.0
scipy>=1.11.0
joblib>=1.3.0          # model persistence (fallback when joblib is preferred over pickle)

# Gradient boosting (5G supervised models)
xgboost>=2.0.0

# Deep learning — Autoencoder (6G anomaly detection)
# CPU-only install:  pip install tensorflow-cpu>=2.13.0
# GPU install:       pip install tensorflow[and-cuda]>=2.13.0
tensorflow>=2.13.0
